//programa que converte uma fração em um número decimal. 

#include <iostream>
using namespace std;

int main() {

    int = a, b; //as variáveis, sendo "a" o númerador e "b" e denominador;  

    cout << "Entre com uma fração (numerador e denominador): "; //mensagem de saída
    cin >> a >> b //usuário entra com os valores de "a" e "b"

    if (b != 0) //se b for diferente de 0, então... 
        cout << "A fração em decimal eh " << 1.0 * a / b << endl; //
    else 
        cout << "Erro: Denominador zero\n "
}